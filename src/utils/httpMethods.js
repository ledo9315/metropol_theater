export const isGet = (req) => req.method === "GET";
export const isPost = (req) => req.method === "POST";
export const isUpdate = (req) => req.method === "UPDATE";
export const isDelete = (req) => req.method === "DELETE";
